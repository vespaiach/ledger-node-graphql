overwrite: true
schema: "src/schema/schema.graphql"
documents: null
generates:
  src/schema/types.generated.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  ./graphql.schema.json:
    plugins:
      - "introspection"
